import Login from "../organism/Login"
import { Header } from "../templates/Header"

export const LoginPage = () => {
    return(
        <>
            <Header/>
            <Login/>
        </>
    )
}